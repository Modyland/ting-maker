final RegExp phoneNumberRegex = RegExp(r'^01[0-9]{8,9}$');
final RegExp eightRegex = RegExp(r'.{8,}');
final RegExp enRegex = RegExp(r'.*[a-zA-Z].*');
final RegExp numRegex = RegExp(r'.*\d.*');
final RegExp specialRegex = RegExp(r'.*[!@#\$%^&*].*');
